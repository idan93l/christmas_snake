const canvasX = 1000
const canvasY = 1000
const initialSnake = [ [ 4, 10 ], [ 4, 10 ] ]
const initialPresent = [ 14, 10 ]
const scale = 50
const timeDelay = 100

export default {canvasX, canvasY, initialSnake, initialPresent, scale, timeDelay};